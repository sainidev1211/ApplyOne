import { Body, Controller, Get, Param, Patch, Query, UseGuards } from '@nestjs/common'; import { JwtAuthGuard } from '../../auth/guards/jwt-auth.guard.js'; import { RolesGuard } from '../../auth/guards/roles.guard.js'; import { Roles } from '../../auth/decorators/roles.decorator.js'; import { AdminSubscriptionsService } from '../services/admin-subscriptions.service.js';
@Controller('admin/subscriptions') @UseGuards(JwtAuthGuard,RolesGuard) @Roles('ADMIN') export class AdminSubscriptionsController {constructor(private s:AdminSubscriptionsService){} @Get() list(@Query()q:any){return this.s.list(q)} @Get(':id') detail(@Param('id')id:string){return this.s.detail(id)} @Patch(':id') update(@Param('id')id:string,@Body()d:any){return this.s.update(id,d)}}
